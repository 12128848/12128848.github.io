---
layout: post
title: "Bugku WEB1 滑稽 Writeup"
date: 2026-09-13 23:20:00 +0800
tags: [CTF,WEB,Bugku]
---

## 一、题目信息
题目名称：滑稽（WEB1）
题型：WEB
> Bugku在线靶机为临时环境，重启环境后访问IP会变化，闲置超时会自动关闭。

## 二、解题思路
访问靶机页面，页面只有大量滑稽表情，没有任何文字提示。
考点：**查看网页源代码**。flag存放在HTML注释 `<!-- -->` 内，HTML注释不会渲染展示在网页界面，只能读取页面源码查看。

## 三、解题过程
1. 在Bugku平台启动靶机环境，访问靶机地址，页面加载出满屏滑稽表情。
2. 使用快捷键 `Ctrl+U` 打开网页源代码。
3. 使用 `Ctrl+F` 搜索关键词`flag`，定位到HTML注释内的flag。
4. 复制flag提交平台，验证答案正确。

## 四、EXP
无脚本，查看网页源代码。

## 五、运行结果
Flag：`flag{d94c75ab301d7b9e8739ac758b8b357e}`

## 六、总结
WEB入门基础题，用来学习CTF最基础操作：查看网页源代码。
很多入门Web题目会把flag放在HTML注释中，注释内容不会在浏览器页面显示，只能通过源码查看。
> 踩坑记录：搜索flag时会匹配到JS代码中的变量touchFlag，这不是答案，需要找到HTML注释形式的flag。